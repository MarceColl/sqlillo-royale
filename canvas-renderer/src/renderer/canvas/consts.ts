import { Settings } from "./types";

export const DEFAULT_SETTINGS: Settings = {
    renderScaling: 1,
};

export const TICK_RATE_MS = 30 / 1000;
export const PLAYER_SIZE = 5;
export const BULLET_SIZE = 2;
